---
description: 完成架构待办后必须勾选 TODO.md
alwaysApply: true
---

# 架构 TODO 自动勾选

`TODO.md` 是耦合清理的进度表。

做完并验证某条待办后，立刻把对应行从 `- [ ]` 改成 `- [x]`，必要时把条目移到「已完成」。不要只在对话里说做完了却不改文件。

只勾选当前改动真正覆盖的条目。部分完成就拆成已完成 + 仍待办，或保持未勾并在条目里注明剩余范围。

不要勾「不要做」一节。不要为了勾选去动协作取色、`native.rs`、窗口焦点或 Photoshop 第一笔路径。
