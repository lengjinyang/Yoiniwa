# Yoiniwa 项目开发约束

## 最高优先级：Photoshop 协作模式第一笔必须有效

“在协作模式中从 Yoiniwa 取色后，回到 Photoshop 的第一笔必须能够正常绘画”是本项目不可破坏的核心功能，优先级高于置顶方式、任务栏层级、窗口外观和其他交互优化。

任何涉及以下区域的修改，都必须首先评估并保护这条不变量：

- 协作模式、始终置顶、任务栏前后层级和窗口锁定。
- `WS_EX_NOACTIVATE`、鼠标穿透、`SetWindowPos`、`setAlwaysOnTop`、窗口焦点和 DWM 状态。
- Windows Ink、数位板、Pointer Events、原生输入 Hook 和 Pointer Capture。
- Photoshop COM 同步、颜色提交队列以及 Alt + 笔尖取色状态机。

必须遵守：

- Photoshop 在协作取色期间始终保持数位板输入归属；Yoiniwa 不得抢占 Photoshop 焦点或破坏 Windows Ink 的目标状态。
- 取色松笔及颜色提交期间，禁止切换焦点、重复调整窗口 Z 序、提升/降低任务栏层级或刷新非客户区。
- 禁止模拟鼠标点击、移动系统光标、伪造 Alt/键盘松开消息或向 Photoshop 注入输入。
- 快速连续多次取色后，Photoshop 的下一次真实笔尖按下仍必须是可绘画的第一笔。
- 修改窗口、输入或 Photoshop 桥接代码时，不得顺带重构无关功能；采用最小改动并保留右键移动、普通置顶、缩放和协作模式入口等现有行为。
- 如果某个方案无法保证第一笔有效，应放弃该方案；不得用任务栏闪烁、偶发丢笔或要求用户多画一笔作为可接受的折中。
- 未经真实 Windows Ink 数位板确认，不得宣称该核心问题已经完全修复。自动检查和鼠标模拟不能替代数位板实机确认。

后续开发中一旦发现改动导致 Photoshop 第一笔失效，应立即停止新增功能，优先恢复这一核心不变量。
