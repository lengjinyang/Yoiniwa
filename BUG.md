# RefCanvas 已知问题

本文档记录 2026-07-29 代码审查中发现、尚待修复的问题。优先级含义：

- **P1**：高优先级，可能导致核心功能失效、资源耗尽或数据不可用，应尽快修复。
- **P2**：中优先级，会在特定条件下造成错误或不稳定，应安排修复并补充回归测试。

## BUG-001：缓存迁移后图片 Worker 仍引用旧路径

- **优先级**：P1
- **位置**：`electron/application.ts:810`
- **状态**：已修复（2026-07-29）

### 问题

缓存迁移完成后，主进程会更新 `cacheRootOverride`、`derivedCache` 和 `assetRegistry` 中的缓存路径，并删除旧缓存目录。但 `imageWorkerAssets` 仍保留已经完成的注册 Promise，图片 Worker 子进程中的 `assetSources` 也仍指向旧文件路径。

### 影响

- 后续图片任务会误以为资源已经注册，不会使用新路径重新注册。
- 缩略图生成失败时可能退回应急预览，但金字塔和 Tile 生成没有等价降级路径，可能导致高缩放级别图片空白。
- 问题通常会持续到应用重启。
- 迁移期间正在运行的任务还可能与旧目录删除操作发生竞争。

### 建议修复

在删除旧缓存目录前停止或排空图片任务，终止或重启 Worker，并清空 `imageWorkerAssets` 与待处理请求状态。迁移后让资源从 `assetRegistry` 中的新路径按需重新注册。

### 回归验证

1. 导入大图并触发缩略图、金字塔和 Tile 生成。
2. 在应用不重启的情况下更改缓存位置。
3. 继续缩放到需要高分辨率 Tile 的级别。
4. 确认 Worker 使用新路径，图片正常显示，旧目录删除后无 `ENOENT` 错误。

## BUG-002：场景包解压没有资源大小限制

- **优先级**：P1
- **位置**：`electron/services/scene-packages.ts:71`
- **状态**：已修复（2026-07-29）

### 问题

`manifest.json` 会通过 `buffer()` 完整读入内存。资源条目则在检查声明的 `byteLength` 和哈希之前完整写入临时文件。当前没有限制 Manifest 大小、资源数量、单个资源解压大小或场景包总解压大小。

### 影响

恶意或严重损坏的 `.refcanvas` 文件可通过压缩炸弹或超大资源耗尽内存、占满磁盘，导致应用崩溃或系统不可用。通过文件关联打开场景时同样会进入该路径。

### 建议修复

- 在读取 Manifest 前检查条目的声明解压大小并设置硬上限。
- 验证资源记录数量以及 `byteLength` 的类型、整数范围和单项上限。
- 在流式解压过程中累计单资源和总字节数，超过上限立即中止并删除临时文件。
- 不应仅信任 ZIP 元数据或 Manifest 中声明的大小。

### 回归验证

增加以下场景包测试：超大 Manifest、资源数量超限、单资源超限、总资源大小超限、声明大小小于实际解压大小，以及正常边界大小的合法场景。

## BUG-003：旧图片请求可能删除新的缓存项

- **优先级**：P2
- **位置**：`src/imageResources.ts:159`
- **状态**：已修复（2026-07-29）

### 问题

图片请求失败时，`onerror` 使用 URL 对缓存执行无条件删除。并发预加载或重试可能已经为同一 URL 写入新的成功条目，此时旧请求稍后失败会误删替代条目。

释放回调也会通过 URL 重新读取当前缓存项，而不是操作最初保留的条目。因此，旧请求的清理可能错误递减新条目的引用计数，并将仍在使用的图片标记为未引用。

### 影响

- 已成功加载的预览可能被旧请求删除。
- 引用计数可能低于真实使用数量。
- 在缓存压力下，仍被组件使用的图片可能被设置为空地址并移除，表现为间歇性空白或重复加载。

### 建议修复

让错误处理和释放回调捕获最初的 `entry`，并仅在 `cache.get(src) === entry` 时删除 Map 项。引用计数和监听器清理应修改捕获的条目，不能通过 URL 操作后来创建的替代条目。

### 回归验证

构造同一 URL 的失败请求与成功预加载交错完成的测试，确认旧请求的 `onerror` 和 release 不会删除新条目，也不会减少新条目的引用计数。

## BUG-004：同尺寸的损坏缓存会取代场景中的正确资源

- **优先级**：P2
- **位置**：`electron/services/scene-packages.ts:93`
- **状态**：已修复（2026-07-29）

### 问题

读取场景包时，资源已经被解压到临时文件并通过 SHA-256 校验。但是，如果目标缓存文件与资源字节数相同，代码会删除已验证的临时文件并继续使用现有缓存，而不校验现有缓存的哈希。

### 影响

现有缓存若被破坏或篡改但长度不变，合法场景仍会加载错误内容或显示空白。后续保存场景时，还可能把错误缓存重新写入场景包，使问题持续存在。

### 建议修复

优先使用刚刚通过哈希验证的临时文件原子替换目标缓存。若希望避免重复写入，则必须先计算并验证现有缓存的 SHA-256，不能只比较文件大小。

### 回归验证

预先创建一个长度正确但内容和哈希错误的缓存文件，再打开包含正确资源的场景包。确认缓存被正确资源替换，图片可正常读取，并且重新保存的场景包能够通过哈希校验。

## 当前检查基线

记录问题时，以下检查均通过：

- TypeScript renderer 与 Electron 类型检查
- ESLint（零警告）
- Knip 死代码检查
- Vitest：42 个测试文件、161 项测试

这些检查尚未覆盖上述并发、缓存迁移、缓存损坏和恶意场景包边界条件。
